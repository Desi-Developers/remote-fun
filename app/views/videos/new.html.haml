.form-inputs
  -if @video.errors.present?
    %ul
      - @video.errors.full_messages.each do |message|
        %li= message
  = form_for(@video, url: videos_path, method: :post) do |f|
    = f.label :title
    = f.text_field :title
    = f.label :description
    = f.text_field :description
    = f.file_field :file, required: true, accept: "video/*"
    = f.submit